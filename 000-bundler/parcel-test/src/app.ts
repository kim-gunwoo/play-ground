import { getPasslinkUrl } from './utils';

// HTML 문서의 로딩이 완료된 후 실행되는 코드
document.addEventListener('DOMContentLoaded', function () {
    // "Hello, World!" 메시지를 콘솔에 출력합니다.
    console.log('Hello, World!', getPasslinkUrl());
    console.log('Hello, World!');
});
