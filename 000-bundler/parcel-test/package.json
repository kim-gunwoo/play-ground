{
  "name": "percel-test",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint",
    "dev": "parcel",
    "start": "parcel serve src/index.html --open",
    "start:stg": "cross-env NODE_ENV=staging parcel serve src/index.html --open",
    "start:prod": "cross-env NODE_ENV=production parcel serve src/index.html --open",
    "build": "parcel build index.html",
    "build:stg": "cross-env NODE_ENV=staging parcel build src/index.html --dist-dir dist/staging --no-source-maps --public-url ./",
    "build:prod": "cross-env NODE_ENV=production parcel build src/index.html --dist-dir dist/production --no-source-maps --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.1.1",
    "@types/node": "^20.12.7",
    "buffer": "^6.0.3",
    "cross-env": "^7.0.3",
    "crypto-browserify": "^3.12.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-react": "^7.34.1",
    "globals": "^15.0.0",
    "parcel": "^2.12.0",
    "typescript-eslint": "^7.7.1"
  }
}
